name: music_tool
description: 音乐处理工具
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  permission_handler: ^11.0.1
  ffmpeg_kit_flutter: ^6.0.3

flutter:
  uses-material-design: true